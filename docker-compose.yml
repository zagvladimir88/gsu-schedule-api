version: '3'

services:
  postgres:
    image: postgres:16
    container_name: ggu-bd-postgres-container
    ports:
      - "5435:5432"
    environment:
      POSTGRES_DB: ggu_bot_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1111